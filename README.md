# EdgeQuery
A library for active learning on graphs, specialized to link prediction: instead of passively training a GNN on whatever edges you have, it decides which missing edge to query/label next to most improve a downstream task per unit labeling cost
